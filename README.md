# Socket-Programming-and-Reliable-Data-Transfer
For demonstrating TCP and UDP concepts. The UDP code is designed to gain some of TCP's reliability.

-----
You must run the server file and THEN the corresponding client file. Further instructions are provided once you do this.
The server will terminate once the client quits.
